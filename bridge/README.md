# Local bridge

Static GitHub Pages cannot execute arbitrary native flash utilities. A localhost bridge is
the appropriate architecture for authorized/documented service tools. Never expose a native
flashing endpoint directly to the public internet.
